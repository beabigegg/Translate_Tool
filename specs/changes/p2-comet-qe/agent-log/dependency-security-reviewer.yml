agent: dependency-security-reviewer
change-id: p2-comet-qe
status: changes-required-applied
artifacts:
  - type: packages-reviewed
    pointer: "unbabel-comet 2.2.7 (Apache-2.0, CLEAN); torch (transitive, BSD-3); pytorch-lightning (transitive, Apache-2.0); transformers (transitive, Apache-2.0); sentencepiece (transitive, Apache-2.0); protobuf (transitive, BSD-3); entmax (transitive, MIT)"
  - type: cve-floor-pins-added
    pointer: "app/backend/requirements.txt: torch>=2.6.0 (GHSA-53q9-r3pm-6pq6 RCE), pytorch-lightning>=2.6.1 (GHSA-75m9-98v2-hjpm), transformers>=4.52.1 (ReDoS), sentencepiece>=0.2.1 (GHSA-38vq-g6vr-w8wf), protobuf>=4.25.8,<5.0.0 (GHSA-8qvm-5x2c-j2w7)"
  - type: license-warning
    pointer: "contracts/env/env-contract.md QE_MODEL_NAME: CC-BY-NC-SA license warning added for wmt22-cometkiwi-da; legal review required before commercial deployment"
  - type: gpu-dep-check
    pointer: "onnxruntime-gpu NOT in transitive tree of unbabel-comet 2.2.7; layout-detector-dependency-gate passes; CPU-only --extra-index-url documented in requirements.txt comment"
notes: |
  F-8 CC-BY-NC-SA model license is the primary operational risk — documented in env-contract.md, not a code blocker.
  F-2 torch CUDA bloat on Linux is mitigated by CPU-only pip install instruction in requirements.txt comment.
  All CVE floor pins applied to requirements.txt.
