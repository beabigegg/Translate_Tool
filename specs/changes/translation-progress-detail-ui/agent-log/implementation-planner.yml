change-id: translation-progress-detail-ui
agent: implementation-planner
status: done
next-action: none — implementation-plan.md ready; implementation DEFERRED pending explicit user approval in a later session (change-request.md Constraints)
summary: "Wrote implementation-plan.md as an execution packet for the deferred backend-engineer/frontend-engineer. 10 required changes (IP-1..IP-10) mapped to owners; file-level plan pins exact files + code-map line ranges (JobStatus L23-48, JobRecord ~L80 + callback ~L388, translation_service critique loop ~L351-417, routes job-status ~L290-371 incl. two-phase ETA at ~L318-320, TranslationProgress.jsx qualityTier 3-8/component 10-85 rendered via TranslatePage L327). Contract updates: api-contract JobStatus +5 fields + close status_detail/layout_viz_available drift + version bump + regen openapi.yml/json; css-contract + design-tokens + BR-98 + data-shape. Test plan references test-plan.md's 5 new files. CI: zero workflow edits for this change's own surface (ci-gates.md Q1/Q2). Flagged batch-critique-qe-scoring sequencing constraint (land it first / coordinate) using only THIS change's change-request Known Context + design Open Risks — did not read the hard-forbidden sibling path."
amendment: "Judge-phase delta (post-incident) added as IP-11..IP-17 without rewriting IP-1..IP-10. Covers: schemas.py +3 judge Optional fields (current_segment_judge_tier 高/中/低|null, _attempt int|null, _substep scoring/retranslating|null) + `judge` current_stage enum (after warnings L48); quality_judge.py run_judge_loop L278 additive OPTIONAL snapshot_cb=None threaded into _run_judge_loop_impl L319 per-block loop, fired fail-soft (try/except) at scoring sub-step (convert L359-362 comprehension to explicit loop, attempt=iteration+1) and retranslate sub-step (L424-438); job_manager.py L472-518 wires the callback + _translate_fn closure (L493) writes substep=retranslating; routes.py multi-phase ETA phase-3 judge term omitted when JUDGE_ENABLED=false OR provider==deepseek (BR-97); TranslationProgress.jsx reuses existing StageDetailPanel/StageBadge (no new component); contracts: api +3 fields+judge enum, BR-98 rename eta-multi-phase-pipeline, data-shape 8 fields, design-tokens judge-tier + --color-stage-judge, css judge rule; tests: rename test_eta_two_phase→multi_phase + new test_quality_judge_snapshot_callback.py (no-op/invocation/fail-soft). Known Risks: added qa-pipeline-consolidation sequencing/coordination note (did NOT read that forbidden sibling path — rests on task summary only)."
artifacts:
  - { type: plan-written, pointer: "specs/changes/translation-progress-detail-ui/implementation-plan.md" }
  - { type: owner-map, pointer: "backend-engineer, frontend-engineer (both DEFERRED)" }
  - { type: scope-summary, pointer: "IP-1..IP-10 (translate/critique/QE) unchanged; IP-11..IP-17 judge-phase delta added; explicit non-goals: rolling history, new endpoint/SSE, critique perf, useJobPolling change, workflow edits, judge cancellation/timeout FIX (qa-pipeline-consolidation)" }
