agent: test-strategist
run-date: 2026-06-19
status: complete
notes: |
  QA fix-back 2026-06-19: AC-2 tautological tests strengthened.
  TestBoundaryPriority — three existing tests now assert
  chunks[1].elements[overlap_element_count].element_id rather than only len(chunks).
  Added test_paragraph_break_preferred_over_heading (new, per QA Issue 1).
  test-plan.md AC→test mapping updated to match actual implemented function names
  (8 stale names corrected; QA Issue 2).
artifacts:
  - { type: test-plan-path, pointer: "specs/changes/p2-long-doc-chunking/test-plan.md" }
  - { type: tdd-pairs, pointer: "tests/test_doc_chunker.py → app/backend/services/doc_chunker.py" }
  - { type: tdd-pairs, pointer: "tests/test_translation_strategy.py → app/backend/services/translation_service.py (Doc2Doc path)" }
  - { type: tdd-pairs, pointer: "tests/test_env_contract.py → contracts/env/env-contract.md (CHUNK_OVERLAP_TOKENS)" }
  - { type: tdd-pairs, pointer: "tests/test_sentence_mode_consistency.py → app/backend/services/translation_service.py (translate_texts backward-compat)" }
  - { type: coverage-tiers, pointer: "unit, contract, integration, data-boundary, resilience" }
  - { type: mapping-completeness, pointer: "AC-1 through AC-8 all mapped; 33 named tests across 4 files; BR-47–BR-53 and Table O fully covered; AC-2 boundary-priority tests now assert split position not just chunk count" }
