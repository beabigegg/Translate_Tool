agent: qa-reviewer
change-id: p2-table-border-protection
timestamp: 2026-06-19
status: complete

artifacts:
  - { type: gate-results, pointer: "collect: pass, targeted: pass, changed-area: pass, full: pass (587 passed, 4 skipped, 0 failed)" }
  - { type: evidence-quality, pointer: "test-evidence.yml confirmed all required phases passed; pre-fix failing runs present in test-runs/ (reproduction evidence); source fix confirmed at fitz_renderer.py:356 (overlay) and fitz_renderer.py:626-646 (side-by-side)" }
  - { type: decision, pointer: "approved-with-risk" }
  - { type: failure-routing, pointer: "VR-1 vector-fixture follow-up routed to test-strategist (P3); VR-2 XObject doc follow-up routed to backend-engineer (P3)" }
  - { type: residual-risk, pointer: "VR-1 AC-1 integration test vacuously skipped (test.pdf has no vector strokes); VR-2 Form XObject masking edge case undocumented" }

notes: |
  All AC-1–AC-5 met. apply_redactions(graphics=0) is the correct PyMuPDF API for
  text-only redaction preserving vector strokes. Side-by-side masking loop physically
  removes text from content stream (not merely painted over). Golden regression passed
  with zero re-baseline. AC-5 confinement confirmed by TestConfinementNoNewImports.
  Approved-with-risk on VR-1 (no vector fixture for AC-1 integration test) and VR-2
  (Form XObject edge case) — both follow-up items, not blocking.
