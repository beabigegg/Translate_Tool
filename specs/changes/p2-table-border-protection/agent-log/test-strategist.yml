agent: test-strategist
change-id: p2-table-border-protection
date: 2026-06-19
status: complete

artifacts:
  - { type: test-plan-path, pointer: "specs/changes/p2-table-border-protection/test-plan.md" }
  - { type: tdd-pairs, pointer: "tests/test_table_border_protection.py → app/backend/renderers/fitz_renderer.py" }
  - { type: tdd-pairs, pointer: "tests/test_golden_regression.py → app/backend/renderers/fitz_renderer.py (re-baseline only if render output shifts)" }
  - { type: coverage-tiers, pointer: "unit (Tier 0), integration (Tier 3), golden-regression (Tier 3)" }
  - { type: mapping-completeness, pointer: "all 5 AC covered; AC-1/AC-3 share integration class; AC-4 reuses existing golden suite; AC-5 via import-confinement unit test" }

notes: >
  Two distinct bugs drive opposing geometry constraints: overlay mask must shrink
  away from table rule strokes (AC-1/AC-3), while side-by-side right panel must
  expand masking to cover all source text (AC-2). Unit tests isolate each
  geometry path independently. Integration tests use tests/fixtures/test.pdf
  (already in Allowed Paths) and inspect output via fitz page introspection APIs
  (get_drawings, get_text, clip rect) — no pixel-diff comparison, which belongs
  to visual-reviewer.
