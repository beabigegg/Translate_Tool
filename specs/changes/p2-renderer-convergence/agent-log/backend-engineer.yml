agent: backend-engineer
change-id: p2-renderer-convergence
date: 2026-06-18
status: complete (re-invocation: QA fixes applied)

artifacts:
  - { type: files-changed, pointer: "app/backend/renderers/bbox_reflow.py:1-132 (unchanged — already correct)" }
  - { type: files-changed, pointer: "app/backend/renderers/fitz_renderer.py:25-26 (import reflow_document added); fitz_renderer.py:237-360 (_generate_overlay rewritten to call reflow_document and use Placement.bbox for text_rect; quad search retained for redact_rect only)" }
  - { type: files-changed, pointer: "app/backend/renderers/coordinate_renderer.py:18-22 (imports reflow_document + create_text_regions_from_placements); coordinate_renderer.py:100-165 (_render_overlay rewritten to call reflow_document and iterate placements by page)" }
  - { type: files-changed, pointer: "app/backend/renderers/text_region_renderer.py:313-345 (create_text_regions_from_placements added — draw-only helper consuming Placement list)" }
  - { type: tests-added, pointer: "tests/test_renderer_convergence.py::TestLayoutEquivalence (6 tests, AC-4): replaced tautological reflow-vs-reflow assertions with wiring tests (test_both_paths_call_reflow_document, test_reportlab_path_calls_reflow_document) + BR-35 assertions" }
  - { type: tests-added, pointer: "tests/test_renderer_convergence.py::TestEquivalenceGolden (4 tests, AC-7): replaced 2 tautological tests with 4 snapshot-based tests using committed .layout.json files" }
  - { type: tests-added, pointer: "tests/fixtures/golden/pdf/fitz_snapshot.layout.json (new — pre-committed golden snapshot)" }
  - { type: tests-added, pointer: "tests/fixtures/golden/pdf/reportlab_snapshot.layout.json (new — pre-committed golden snapshot)" }
  - { type: test-output, pointer: "28 passed, 0 skipped — tests/test_renderer_convergence.py; cdd-kit phases targeted/changed-area/contract all pass" }
  - { type: contracts-touched, pointer: "n/a (contracts were pre-authored; consumed only, not edited per implementation-plan.md)" }

notes:
  - AC-1 wired: fitz_renderer._generate_overlay now calls reflow_document; Placement.bbox drives text_rect; fitz quad search drives redact_rect only (Open Risk mitigated).
  - AC-1 wired: coordinate_renderer._render_overlay now calls reflow_document; create_text_regions_from_placements is the draw-only bridge (no IR logic in adapter).
  - text_region_renderer.create_text_regions_from_elements retained for backward compatibility (used by fitz side-by-side path); create_text_regions_from_placements is the new overlay path.
  - .layout.json snapshots are pre-committed static files (not auto-generated at test time per ci-gate-contract).
  - TestLayoutEquivalence::test_both_paths_call_reflow_document patches app.backend.renderers.fitz_renderer.reflow_document to assert it is called by _generate_overlay.
  - TestLayoutEquivalence::test_reportlab_path_calls_reflow_document patches coordinate_renderer.reflow_document and mocks Canvas to assert it is called by _render_overlay.
