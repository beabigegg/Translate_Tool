agent: contract-reviewer
status: approved
summary: >
  Approved BR-115 (header-footer-native-collection) with the txbxContent-strip
  precondition, element-identity linked-part dedup, and the disjoint-domain COM
  boundary. No Table T row (would restate BR-81 row 368). Verified every cited
  symbol against live source, including that uniq_texts dedup ordering keeps body
  indices stable (AC-6). schema-version 0.31.0 -> 0.32.0.
artifacts:
  - { type: contracts-reviewed, pointer: "contracts/business/business-rules.md" }
  - { type: contracts-reviewed, pointer: "contracts/CHANGELOG.md" }
  - { type: version-bumps, pointer: "business-rules 0.31.0 -> 0.32.0 (additive BR-115)" }
  - { type: breaking-changes, pointer: "none" }
notes: |
  Reviewer independently confirmed: postprocess_docx_shapes_with_word iterates
  .Shapes only (never header para/table text); _p_text_with_breaks .// reaches into
  txbxContent (strip is load-bearing); _txbx_iter_texts walks doc._element only so it
  never double-counts header textboxes; uniq_texts is text-dedup in segs order and
  header/footer is appended after the body walk, so body indices are stable (AC-6).
  The txbxContent-strip is scoped to the header/footer extraction path; the BR wording
  explicitly leaves the body's pre-existing textbox double-count unchanged.
