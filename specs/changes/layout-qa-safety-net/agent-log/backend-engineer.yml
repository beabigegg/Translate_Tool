artifacts:
  - { type: files-changed, pointer: "app/backend/services/layout_qa.py (new, 312 lines): metric core moved verbatim (BIOU_REGRESSION_BUDGET, _iou, compute_biou, check_residual_text, compute_truncation_rate) + LayoutQAResult + run_layout_qa composition with residual-source disambiguation and fail-soft wrap" }
  - { type: files-changed, pointer: "app/backend/processors/pdf_processor.py:1120-1169 (_dispatch_render seam) — LAYOUT_QA_ENABLED-guarded run_layout_qa(...) call immediately after the L1160 _emit_truncation_disclosure_warning call, PDF->PDF path only" }
  - { type: files-changed, pointer: "app/backend/config.py:178-190 — LAYOUT_QA_ENABLED (default false) and LAYOUT_QA_MAX_BOXES_PER_PAGE (default 500), mirroring LAYOUT_DETECTOR_ENABLED pattern" }
  - { type: files-changed, pointer: "tests/metrics/biou.py, tests/metrics/residual_text.py, tests/metrics/truncation_rate.py — reduced to re-export shims from app.backend.services.layout_qa, preserving _iou and BIOU_REGRESSION_BUDGET" }
  - { type: tests-added, pointer: "tests/test_layout_qa.py::TestBiouRegressionSignal, TestResidualSignal, TestFailSoft, TestDataBoundary, TestMetricCoreIdentity + module-level BR-106/office-absence tests (AC-2..AC-9)" }
  - { type: tests-added, pointer: "tests/test_pdf_render_warnings.py::TestLayoutQaDisabled::test_flag_off_run_layout_qa_not_invoked_no_warning (AC-1, asserts mock.assert_not_called, not just empty warnings)" }
  - { type: tests-added, pointer: "tests/test_pdf_render_warnings.py::TestLayoutQaWarning::test_biou_regression_warning_fires_through_real_seam, ::test_layout_qa_exception_never_fails_job_or_fabricates_warning (AC-2/AC-4, real _dispatch_render seam, real fitz-rendered PDFs, no mock wrapper)" }
  - { type: tests-added, pointer: "tests/test_env_contract.py::TestEnvContractDeclared::test_layout_qa_enabled_declared, ::test_layout_qa_max_boxes_per_page_declared, ::test_layout_qa_enabled_wired_in_config_default_false (AC-6)" }
  - { type: test-output, pointer: "full-suite smoke: 1217 passed, 4 skipped (pre-existing, unrelated fixtures), 0 failed, 146.71s -- specs/changes/layout-qa-safety-net/test-runs/20260708-144406/stdout.log" }
  - { type: contracts-touched, pointer: "contracts/env/env-contract.md, .env.example.template, env.schema.json (LAYOUT_QA_ENABLED/LAYOUT_QA_MAX_BOXES_PER_PAGE, already applied by contract-reviewer -- verified only, not edited this session); contracts/business/business-rules.md BR-106 (verified present, matches code behavior)" }
notes:
  - "Implementation (layout_qa.py, config.py, pdf_processor.py seam, metric shims) and the test files (test_layout_qa.py, test_pdf_render_warnings.py new classes, test_env_contract.py extensions) already existed on disk at session start (prior agent pass, uncommitted). This session's work was: (1) full read of implementation-plan.md/design.md/test-plan.md/ci-gates.md/change-classification.md/context-manifest.md, (2) line-by-line verification of layout_qa.py against design.md L29-39 signature/return contract and the Decision-1..5 requirements, (3) IP-6 orphan-import grep across tests/ and app/ for tests.metrics.*, compute_biou, check_residual_text, _iou, BIOU_REGRESSION_BUDGET -- confirmed only tests/test_layout_metrics.py imports the shim paths and none break, (4) verification that the real seam is _dispatch_render (L1096-1169) per the plan's seam-name correction and that the guard/call are wired exactly as specified, (5) ran the full ADR-0005 bounded test ladder (collect/targeted/changed-area/contract/full) via conda run -n translate-tool, producing test-evidence.yml with final-status passed and zero waivers."
  - "Residual-source disambiguation reads getattr(elem, 'content', '') (the actual TranslatableElement field name), not the plan's illustrative getattr(elem, 'text', '') placeholder -- confirmed correct against app/backend/models/translatable_document.py's real TranslatableElement schema (content/translated_content/bbox/page_num), not a deviation."
  - "No contract files were edited by this agent (per instructions they were already applied); verified byte-consistent defaults (false/500) between config.py and env-contract.md/env.schema.json."
  - "ci-gates.md's residual-text gate (pytest tests/test_pdf_layout_refactor.py -k residual_text) currently has no test name matching the substring 'residual_text' in that file (closest match is test_growth_capped_at_table_budget_residual_truncates_and_warns); this is a pre-existing gate-command/test-name mismatch unrelated to this change's file-level plan (test_pdf_layout_refactor.py is out of scope) -- flagged for qa-reviewer/ci-cd-gatekeeper, not modified here."
