agent: contract-reviewer
status: approved
summary: >
  Approved BR-116 (pptx-group-shape-collection) referencing BR-113 (document-order
  counter, bounded recursion) and BR-81 (id()-recycling hazard) rather than
  duplicating them. No Table T row. Verified GroupShape.shapes, shape_id=id(shape)
  L220, SEGMENT_* constants, MAX_TABLE_NESTING_DEPTH against live source; confirmed
  the restore loop is obj_ref-based so no restore rewrite is implied. 0.32.0 -> 0.33.0.
artifacts:
  - { type: contracts-reviewed, pointer: "contracts/business/business-rules.md" }
  - { type: version-bumps, pointer: "business-rules 0.32.0 -> 0.33.0 (additive BR-116)" }
  - { type: breaking-changes, pointer: "none" }
