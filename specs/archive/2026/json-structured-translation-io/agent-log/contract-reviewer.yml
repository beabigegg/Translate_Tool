change-id: json-structured-translation-io
timestamp: 2026-07-10
agent: contract-reviewer
status: changes-required-applied
summary: >-
  Three contracts rewritten. Data-shape wire format replaced with a coordinate
  JSON cell list and given the consumers table it never had; BR-79/80/82/83
  amended; BR-111 and BR-112 added; BR-108 widened rather than retired; env
  kill-switch synced across all three env files.
artifacts:
  - { type: contracts-reviewed, pointer: "contracts/data/data-shape-contract.md" }
  - { type: contracts-reviewed, pointer: "contracts/business/business-rules.md" }
  - { type: contracts-reviewed, pointer: "contracts/env/env-contract.md" }
  - { type: version-bumps, pointer: "data 0.17.4 -> 0.18.0; business 0.29.0 -> 0.30.0; env 0.18.0 -> 0.19.0 (all minor)" }
  - { type: breaking-changes, pointer: "table wire format fully replaced; internal-only, all consumers change atomically, kill-switch reverts byte-for-byte" }
  - { type: consumers-impacted, pointer: "app/backend/utils/table_serializer.py" }
notes: >-
  Substantive design correction accepted and applied: design.md scoped BR-108 to
  the plain-text fallback branch only, which would let a meta-refusal wrapped in
  well-formed, schema-valid, non-echoed JSON (e.g. {"translation": "I need more
  context to translate this"}) pass every BR-112 structural check on the happy
  path and be written straight to output. contract-reviewer generalized the live
  probe's Finding 4 ("schema-valid is not translated") to "schema-valid is not
  non-refusal" and required BR-108 on the value ACTUALLY WRITTEN, whichever branch
  produced it. Verified by main Claude against design.md L29 and L88, which do say
  "keep BR-108 guard on fallback".
  Also verified before applying: the BR-80 slug `table-instruction-before-grid`
  appears nowhere outside its own row, so the rename is safe; the stale
  `serialize(cells) -> str` row in the TableStructure consumers table was updated;
  live schema-versions were re-read from disk before every bump. Neither
  absence-tested token is present.
  Deferred by spec-architect and NOT applied: flipping ADR-0006 from `proposed` to
  `superseded` on the strength of a still-`proposed` ADR-0017. Recorded in both
  ADRs instead.
next-action: run-test-strategist
