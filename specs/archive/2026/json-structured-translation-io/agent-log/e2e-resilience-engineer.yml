agent: e2e-resilience-engineer
change-id: json-structured-translation-io
status: completed-with-blocked-subitem
artifacts:
  - { type: test-files, pointer: "tests/test_table_context_translation.py::TestHostileTableJsonReplies (new class, 12 tests)" }
  - { type: test-files, pointer: "tests/test_json_translation_body.py::TestBodyFallback (extended) + TestBodyFallbackReasonLogging (new class, 12 tests total added)" }
  - { type: scenarios-covered, pointer: "table: empty content, unparseable (truncated/trailing-garbage/plain-prose), missing 'cells' key, malformed cell (missing field / non-int coord), missing-sent-coordinate (no partial assignment), extra-coordinate-ignored (no fallback), echoed-whole-grid (fallback), single-cell-echo-legitimate (no fallback, WHICH-condition asserted), job-never-raises sweep, per-trigger INFO log with exact BR-82 reason substring" }
  - { type: scenarios-covered, pointer: "body: unparseable JSON, empty content (both ok=False and ok=True variants), missing translation key, wrong-type translation (int/list/none/dict), echoed source, meta-refusal-inside-valid-JSON (BR-108), job-never-raises sweep, per-trigger INFO log asserting the '[JSON-BODY] fallback to plain-text translate_once:' prefix + non-empty reason" }
  - { type: mutation-checks, pointer: "table echoed-source: disabled the `if sent_cells and all(...)` echo-check in table_serializer.py::parse_json (Edit-tool toggle, never git checkout) -> tests/test_table_context_translation.py::TestHostileTableJsonReplies::test_echoed_whole_grid_falls_back went RED (AssertionError: assert 0 >= 1, not a collection/import error) -> restored -> GREEN (68/68 in test_table_serialization.py + test_table_context_translation.py)." }
  - { type: mutation-checks, pointer: "body meta-refusal: disabled `if is_meta_refusal(translated, text):` in translation_helpers.py::translate_merged_paragraphs (changed to `if False and ...`) -> tests/test_json_translation_body.py::TestBodyFallback::test_meta_refusal_inside_valid_json_still_caught went RED (AssertionError: results == [(True,'I need more context to translate this')] != [(True,'Executive Summary:')]) -> restored -> GREEN (46/46 in test_json_translation_body.py + test_nontranslatable_segment_guard.py)." }
  - { type: trace-artifacts, pointer: "n/a (no UI surface; backend pytest evidence only, see commands below)" }
notes: |
  CER-004 filed (specs/changes/json-structured-translation-io/context-manifest.md) for
  read access to app/backend/utils/json_translation.py — production source needed per
  the task header's explicit Read list but absent from context-manifest.md Allowed
  Paths / the e2e-resilience-engineer packet. tests/test_json_translation_body.py
  auto-approved under context-policy.json's test-path auto-safe rule; the production
  module (app/backend/utils/json_translation.py) remains status: pending, requiring
  explicit `cdd-kit context approve json-structured-translation-io CER-004`.
  Worked around the block by testing the body-path fallback contract as a black box
  through the already-allowed app/backend/utils/translation_helpers.py seam (calling
  the real json_translation module via the existing import chain, never opening its
  source with the Read tool) — every body-path assertion in this log is grounded in
  translation_helpers.py source actually read (the `_translate_body_json` function
  body, including its exact log-line format), not in guessed json_translation.py
  internals.

  INCIDENT (self-caused, corrected same session): during the table-path falsifiability
  check I ran `git checkout -- app/backend/utils/table_serializer.py` to "restore" after
  a sed-based toggle. This was wrong — the JSON serialize_json/parse_json functions
  were uncommitted working-tree-only content (not yet in any commit), so the checkout
  reverted the file to the pre-change committed HEAD and silently deleted them. Caught
  immediately by the next test run (AttributeError: no attribute 'serialize_json').
  Recovered by reconstructing the exact original content via the Edit tool (Bash
  heredoc rewrite was blocked by the auto-mode classifier as irreversible-destruction
  risk) from the verbatim content already captured earlier in the same session's Read
  tool output. Verified recovery via: (1) `git diff --stat` matching the pre-incident
  130-line change size exactly, (2) full green re-run of both affected test files
  (68/68), (3) full suite green (1361 passed, 4 skipped — the reported 1337 baseline
  plus exactly the 24 tests added in this task). All subsequent falsifiability toggles
  in this session used Edit-tool disable/restore only, never git checkout, on files
  under active edit.
commands:
  - "conda run -n translate-tool python -m pytest tests/test_table_context_translation.py::TestHostileTableJsonReplies -v"
  - "conda run -n translate-tool python -m pytest tests/test_json_translation_body.py -v"
  - "conda run -n translate-tool python -m pytest tests/test_table_serialization.py tests/test_table_context_translation.py tests/test_json_translation_prompt.py tests/test_json_translation_body.py tests/test_nontranslatable_segment_guard.py tests/test_pdf_layout_table_fixes.py tests/test_llm_client_protocol.py tests/test_openai_compatible_client.py tests/test_ollama_client_dynamic_strategy.py tests/test_context_window_segments.py"
  - "conda run -n translate-tool python -m pytest tests/ -q"
full-suite-result: "1361 passed, 4 skipped, 0 failed (baseline 1337 + 24 new tests)"
