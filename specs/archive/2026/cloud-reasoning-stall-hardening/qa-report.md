# QA Report — cloud-reasoning-stall-hardening

## Decision: APPROVED

## Gate Results
- `cdd-kit validate --contracts` → All validations passed (API conformance, response shape, env semantic).
- `test-evidence.yml`: `final-status: passed`; required phases collect/targeted/changed-area/contract all `passed`; non-required `full` smoke `passed`; no waiver fields; all runs under this change's `test-runs/`, generated by `cdd-kit test run`.
- Full suite (`test-runs/20260711-164545`): 1481 passed, 4 skipped, 1 xfailed, 0 failures/0 errors. The lone XFAIL (`test_length_guard.py` FP-boundary) is a pre-existing tracked gap, unrelated.
- Evidence freshness: evidence mtime (16:48) and runs (16:44:58+) postdate the latest source edit (`openai_compatible_client.py` 16:44:14).

## AC Coverage (all at outgoing-payload / real-selection boundary)
- **AC-1 (BR-118 directive + no leak):** `TestReasoningDirectiveComposition` — exact-equality on the outgoing SYSTEM message (`== f"{directive}\n\n{base}\n\n{neighbor}"`) for translate_once; for translate_json `user_messages[0]["content"] == json_payload` + directive-absent-from-every-user-message. Constant-sourcing proven by monkeypatching `OPENAI_TRANSLATION_REASONING`→`high`. Sabotage-verified (remove prepend → 3 RED with assertion/IndexError; restored byte-identical).
- **AC-2 (outline exemption):** `test_complete_passes_reasoning_none_...` — complete() emits NO system message and NO `Reasoning:` substring. `_REASONING_UNSET` sentinel distinguishes omission (→ config default) from explicit `None` (outline).
- **AC-3 / AC-4 (BR-100 ceiling + bounded embed):** resilience tests patch the ceiling to 0.5s and assert measured wall-clock `< ceiling*5` AND `>= ceiling*0.5` (defeats trivial fast-pass), on the real returned value/elapsed. `TestEmbedBounded` proves embed() has no raw-`session.post` fallback when `_run_bounded_post` is stubbed. Sabotage-verified (unbound embed → 10s vs 0.5s ceiling RED; restored byte-identical).
- **AC-5 (BR-119 critique skip):** SET-membership selection assertions — default-off both segments enter (byte-identical); opt-in the cache-HIT segment is excluded AND its Phase-1 draft is retained in `tmap` (no drop/empty). Not selection-by-count.

## Breaking Change (disclosed, intentional)
`OPENAI_TOTAL_TIMEOUT_SECONDS` default 480→120 is a documented breaking default-value change: env-contract row (with the 120s-connect-leg calibration NOTE), `.env.example.template`, `config.py` comment, and CHANGELOG `[env 0.20.0]` / `[business 0.35.0]`. Deployments not overriding it inherit 120s on upgrade.

## Contract / Version Hygiene
business-rules 0.35.0 + env-contract 0.20.0, each with a matching CHANGELOG entry citing this change; validator green.

## Residual Risks (documented, non-blocking)
1. **Ceiling calibration tension** — 120s equals the 120s connect-timeout leg; a legitimate cold-start connect could theoretically brush the ceiling. Probed legit calls complete 3–16s (~7x headroom). Mitigation: env override. Owner: application-team.
2. **Item-4 default-off** — `CRITIQUE_SKIP_CACHED_SEGMENTS=false` by default means no critique-cost reduction until an operator opts in; opting in serves an un-critiqued draft for a segment cached from a run interrupted before its critique persisted. Explicit documented operator trade (BR-119), not a defect. The default-on stall/quality fix comes from items 1–3 (reasoning-off + lowered ceiling + bounded embed).

## Failures / Fixback
None.
