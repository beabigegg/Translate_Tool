change-id: fallback-chain-cloud-providers
agent: ci-cd-gatekeeper
timestamp: "2026-06-20T00:00:00Z"
status: complete
artifacts:
  - { type: tiers-modified, pointer: "1" }
  - { type: gate-promotions, pointer: "n/a (no existing gate to promote; new change gate added at Tier 1)" }
  - { type: workflow-files-changed, pointer: ".github/workflows/contract-driven-gates.yml:46-66" }
  - { type: required-status-checks, pointer: "contract-and-fast-tests" }
next-action: none
notes: |
  Three steps added to the existing contract-and-fast-tests job (all Tier 1):
  1. Change gate step updated: echo replaced with `cdd-kit gate fallback-chain-cloud-providers`.
  2. Env schema sync step: grep-based check that DEEPSEEK_ENABLED is in both
     contracts/env/.env.example.template and contracts/env/env.schema.json.
  3. Targeted tests step: pytest on test_provider_fallback.py + test_env_contract.py with -x.
  No new jobs or runners. Full pytest step already present — unchanged.
  ci-gates.md written to specs/changes/fallback-chain-cloud-providers/ci-gates.md.
