agent: contract-reviewer
change-id: quality-metrics-gating
status: approved-with-risk
summary: >
  All 5 contract surfaces pass. One required text fix applied (data-shape-contract.md:762 stale
  CER-003 wording replaced with seam-only). Known risk: QE_ENABLED default-flip uses minor
  version bump rather than major — accepted given BR-90 safe-degrade and pre-1.0 convention.
  ci-gates.md env-sync grep already covers QE_RESCORE_THRESHOLD.
artifacts:
  - type: contracts-reviewed
    pointer: "contracts/env/env-contract.md, contracts/env/env.schema.json, contracts/env/.env.example.template, contracts/business/business-rules.md, contracts/data/data-shape-contract.md"
  - type: version-bumps
    pointer: "env-contract 0.11.0; business-rules 0.21.0; data-shape 0.15.0 — all bumped with last-changed 2026-06-27"
  - type: breaking-changes
    pointer: "QE_ENABLED default false→true is breaking per policy; mitigated by BR-90 safe-degrade; minor bump accepted"
  - type: required-fix
    pointer: "contracts/data/data-shape-contract.md:762 — seam-only wording applied"
