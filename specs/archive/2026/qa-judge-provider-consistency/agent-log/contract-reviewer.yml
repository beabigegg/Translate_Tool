change-id: qa-judge-provider-consistency
agent: contract-reviewer
status: done
next-action: spec-architect writes design.md (client/model-role decision)
summary: "New rule is BR-98 (judge-retranslation-provider-consistency), appended after BR-97 in Rule Inventory (business-rules.md:109). Extends D4's 'judge scoring uses own client, never model_router' precedent (quality_judge.py:3-4,57-60) and BR-95's per-sub-call dedicated-client pattern. No new env var needed — JUDGE_PROVIDER/JUDGE_CLOUD_PROVIDER_ID/JUDGE_MODEL already documented (env-contract.md:45-48), though those rows currently slightly overclaim run_judge_loop's re-translation sub-path — recommend a (BR-98) cross-reference once the rule lands."
artifacts:
  - { type: contracts-reviewed, pointer: "contracts/business/business-rules.md" }
  - { type: contracts-reviewed, pointer: "contracts/env/env-contract.md" }
  - { type: recommended-br-number, pointer: "BR-98" }
