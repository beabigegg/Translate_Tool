agent: test-strategist
change-id: p1-provider-routing
date: 2026-06-17
status: complete

artifacts:
  - { type: test-plan-path, pointer: "specs/changes/p1-provider-routing/test-plan.md" }
  - { type: tdd-pairs, pointer: "tests/test_model_router.py → app/backend/services/model_router.py" }
  - { type: coverage-tiers, pointer: "unit (tier 0), contract (tier 1)" }
  - { type: mapping-completeness, pointer: "AC-1 through AC-7 all mapped; AC-7 is contract-only (business-rules.md update)" }

notes: |
  Existing TestConfigDrivenRouting::test_hardcoded_routing_table_removed already covers AC-1
  structurally but checks _ROUTING_TABLE — backend-engineer must confirm the symbol name matches
  _OLLAMA_ROUTING_TABLE (the actual name in model_router.py). TestResolveRouteGroups::
  test_vietnamese_japanese_german_gives_one_group may need updating once per-language resolution
  splits that batch differently; recorded in Test Update Contract table.
