agent: implementation-planner
change-id: p1-provider-routing
date: 2026-06-17
status: complete

artifacts:
  - { type: plan-written, pointer: "specs/changes/p1-provider-routing/implementation-plan.md" }
  - { type: owner-map, pointer: "backend-engineer (IP-1..IP-6)" }
  - { type: scope-summary, pointer: "in: model_router.py, providers.yml(+.example), test_model_router.py; out: routes.py, job_manager.py, orchestrator.py, translation_service.py, workflows" }

notes: |
  CER-001 resolved: orchestrator.py and translation_service.py do NOT call
  resolve_route_groups(). Real consumers are app/backend/api/routes.py:129
  (create_job) and app/backend/services/job_manager.py:221/279 — both already
  multi-group safe (iterate route_groups, multi_group flag, per-target output
  suffixes). routes.py uses route_groups[0].model_type only as a representative
  value for num_ctx VRAM bounds; unaffected by group count. No consumer change
  needed; emitting 1..N groups is already supported.

  Shape discrepancy flagged for contract-reviewer: BR-4 prose says routing.rules
  is "a list of {lang, model, provider, profile} entries" but test-plan.md
  Fixture Shape uses a language-keyed map. Plan implements the map-keyed shape
  (tests are executable spec) and routes the wording reconciliation to
  contract-reviewer. Did not edit BR-4.
