agent: ui-ux-reviewer
change-id: expose-output-mode-ui
status: approved-with-notes
summary: Selector placement, gating, default, and payload wiring all correct; pre-existing accessibility issue in Select.jsx (no label-for association); minor copy suggestions
artifacts:
  - type: journeys-reviewed
    pointer: "step-2 translate mode selector, extraction_only gating, job reset/restore"
  - type: accessibility-findings
    pointer: "pre-existing: Select.jsx label has no htmlFor/id; WCAG 1.3.1 failure on all selects including new one"
  - type: copy-issues
    pointer: "append label omits translation position; replace label has redundant slash — not blocking"
notes: No new regressions. Pre-existing Select.jsx accessibility defect should be fixed in a separate change.
