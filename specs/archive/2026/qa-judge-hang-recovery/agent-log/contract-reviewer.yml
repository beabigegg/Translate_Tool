change-id: qa-judge-hang-recovery
agent: contract-reviewer
status: done
next-action: spec-architect writes design.md (cancellation mechanism, timeout placement, cancelled-vs-unavailable decision)
summary: "BR-74 can extend in-place for the timeout case (fits 'any exception' wording); cancellation is NOT an error condition so needs new wording or a new rule, not a verbatim BR-74 reuse. JudgeResult.judge_status enum today is only available/disabled/unavailable (data-shape-contract.md:567) — no 4th value exists. If adding one, this repo's job-level cancellation precedent uses 'stopped' (JobStatus.status), not 'cancelled'. GET /jobs/{id}/judge enum extension would be additive/non-breaking per compatibility policy; frontend JudgePanel's condition is a positive match on 'available' so an unhandled new value fails safe. New timeout env var should follow CRITIQUE_TIMEOUT_SECONDS's naming/shape (env-contract.md:35); env.schema.json has no existing float-pattern regex to copy — flagged as a fresh decision, not an oversight."
artifacts:
  - { type: contracts-reviewed, pointer: "contracts/business/business-rules.md" }
  - { type: contracts-reviewed, pointer: "contracts/data/data-shape-contract.md" }
  - { type: contracts-reviewed, pointer: "contracts/api/api-contract.md" }
  - { type: contracts-reviewed, pointer: "contracts/env/env-contract.md" }
