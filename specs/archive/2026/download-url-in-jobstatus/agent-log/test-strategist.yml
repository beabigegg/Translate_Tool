agent: test-strategist
change-id: download-url-in-jobstatus
date: 2026-06-20
status: complete

artifacts:
  - { type: test-plan-path, pointer: "specs/changes/download-url-in-jobstatus/test-plan.md" }
  - { type: tdd-pairs, pointer: "tests/test_provider_fallback.py::TestJobStatusShape → app/backend/api/schemas.py" }
  - { type: tdd-pairs, pointer: "tests/test_jobstatus_download_url.py → app/backend/api/routes.py::job_status" }
  - { type: coverage-tiers, pointer: "unit (tier 0), integration (tier 1), contract (tier 1)" }
  - { type: mapping-completeness, pointer: "AC-1 through AC-7 all mapped; AC-6 covered by cdd-kit validate" }

notes: >
  Derivation rule tests are in a new file to keep TestJobStatusShape focused on
  schema shape only. AC-5 (no field dropped) is proven by running the full
  existing TestJobStatusShape class green — no test body changes required there,
  only two additive methods. CER-001 (response-samples.json) remains pending;
  contract sample update deferred until approved.
