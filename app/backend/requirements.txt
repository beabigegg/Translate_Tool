fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
requests>=2.31
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2
pypdf2>=3.0.1
pymupdf>=1.23.0
lxml>=5.0.0
blingfire>=0.1.8
pysbd>=0.3.4
reportlab>=4.0.0
onnxruntime>=1.20.0
huggingface_hub>=0.22.0
numpy>=1.24.0
pyyaml>=6.0.0
# p2-comet-qe: COMET/xCOMET neural QE (lazy-loaded only when QE_ENABLED=true).
# CPU-only install: use --extra-index-url https://download.pytorch.org/whl/cpu
# to avoid pulling CUDA packages on Linux. onnxruntime-gpu must NOT appear in
# the transitive tree (layout-detector-dependency-gate fails if it does).
# LICENSE NOTE: default model wmt22-cometkiwi-da is CC-BY-NC-SA (non-commercial).
# Legal review required before commercial deployment. See env-contract.md QE_MODEL_NAME.
unbabel-comet
# Transitive floor pins required by dep-security-reviewer (p2-comet-qe):
torch>=2.6.0               # fixes GHSA-53q9-r3pm-6pq6 (RCE via torch.load)
pytorch-lightning>=2.6.1   # fixes GHSA-75m9-98v2-hjpm (checkpoint deserialization)
transformers>=4.52.1       # fixes ReDoS vulns GHSA-37mw-44qp-f5jm et al.
sentencepiece>=0.2.1       # fixes GHSA-38vq-g6vr-w8wf (heap overflow)
protobuf>=4.25.8,<5.0.0    # fixes GHSA-8qvm-5x2c-j2w7 (DoS); stays within unbabel-comet bound
jsonschema>=4.0.0           # required by cdd-kit validate --contracts response-shape validator

# Media STT pipeline (audio/video -> VAD -> STT -> translate; lazy-loaded only
# when the media pipeline is actually invoked, mirrors the COMET section above).
# qwen-asr is the default STT engine (config.STT_ENGINE="qwen3-asr"); torch is
# already a transitive dependency via unbabel-comet above, not repeated here.
# LICENSE: QwenLM/Qwen3-ASR is Apache-2.0 (commercial use permitted) — verified,
# unlike the unbabel-comet default model above which is non-commercial.
qwen-asr
silero-vad
deepfilternet
soundfile
# faster-whisper: OPTIONAL fallback STT engine. Only needed when
# config.STT_ENGINE is switched to "faster-whisper".
faster-whisper

# Testing
pytest>=8.0.0
json-repair>=0.30
httpx2>=0.1.0
